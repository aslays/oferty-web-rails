<p id="notice"><%= notice %></p>

<% if current_user.rol == 1 %>

<div class="contenedor-tabla">
<div class="table-responsive">
<h1>Ciudades</h1>

<table class="table table-condensed">
  <thead>
    <tr class="info">
      <th>Ciudad</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @ciudads.each do |ciudad| %>
      <tr class="active">
        <td><%= ciudad.ciudad %></td>
        <td><%= link_to 'Mostrar', ciudad, class: "btn btn-primary" %></td>
        <td><%= link_to 'Editar', edit_ciudad_path(ciudad), class: "btn btn-warning" %></td>
        <td><%= link_to 'Borrar', ciudad, class: "btn btn-danger", method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Nueva Ciudad', new_ciudad_path, class: "btn btn-success" %>

<% else %>
        <p>Debes iniciar sesión <%=link_to 'Login', usuario_sessions_new_path %></p>
<% end %> 

</div>
</div>